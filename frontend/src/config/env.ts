const config = {
  apiBaseUrl: import.meta.env.VITE_API_BASE_URL ?? 'http://localhost:8080',
  externalApiUrl: import.meta.env.VITE_EXTERNAL_API_URL ?? 'https://projekti-e-9.onrender.com/',
};

export default config;
